import style from "./requestDemoBtn.module.scss";

const RequestDemoBtn = () => {
  return (
    <div className={style.requestDemoBtnWrapper}>
      <button>Request a demo</button>
    </div>
  );
};

export default RequestDemoBtn;
